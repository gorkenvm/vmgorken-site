name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Kodu al
        uses: actions/checkout@v4

      - name: Node.js 20 kur
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Build al (GitHub Actions üzerinde)
        run: npm install && npm run build

      - name: dist/ klasörünü VPS'e kopyala
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: vmg
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "dist"
          target: "/home/vmg/www/vmgorken-site/"
          rm: true

      - name: Dosya izinlerini düzelt
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: vmg
          key: ${{ secrets.VPS_SSH_KEY }}
          script: find /home/vmg/www/vmgorken-site/dist -type f -exec chmod 644 {} \; && find /home/vmg/www/vmgorken-site/dist -type d -exec chmod 755 {} \;
